
{{ form_start(form) }}
    <label for="">Tytuł:</label>
    <br>
    {{ form_widget(form.title) }}
    <br>
    <label for="">Zawartość:</label>
    <br>
    {{ form_widget(form.content) }}
    <br>
    {{ form_widget(form) }}
    <br>
    <button class="btn btn-success">{{ button_label|default('Zapisz') }}</button>
{{ form_end(form) }}
